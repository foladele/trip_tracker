

<h1>Location <%=@location.name%></h1>
<h3><%= @location.description %></h3>
<%= link_to "Add Address", new_address_path(location_id: @location.id) %>

<hr/>
<%= link_to 'All locations', trip_locations_path(@trip) %>
<%= link_to 'Edit', edit_trip_location_path(@trip, @location)%>
<%= link_to 'Delete', trip_location_path(@trip, @location), method: :delete %>
<br/>

<%= link_to 'Show Address', address_path(@location.address) %>
